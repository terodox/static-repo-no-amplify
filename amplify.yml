version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "I HAVE ARRIVED"
    build:
      commands:
        - npm run build
  artifacts:
    # IMPORTANT - Please verify your build output directory
    baseDirectory: src/
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
